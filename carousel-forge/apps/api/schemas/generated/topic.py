"""AUTO-GENERATED — do not edit. `python scripts/codegen.py`로 재생성한다.

source: schemas/topic.schema.json
"""

# generated by datamodel-codegen:
#   filename:  topic.schema.json

from __future__ import annotations

from enum import StrEnum
from typing import Annotated

from pydantic import BaseModel, ConfigDict, Field


class Constraints(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    must_include: list[str]
    must_avoid: list[str]


class SourceFidelity(StrEnum):
    """
    strict이면 원문에 없는 사실을 창작하지 않는다. 문서 입력의 기본값은 strict.
    """

    STRICT = 'strict'
    FLEXIBLE = 'flexible'


class TopicBriefPayload(BaseModel):
    """
    A2 TopicIntake가 어떤 입력(문서/채팅/키워드/URL/시스템 제안)이든 수렴시키는 정규화 형태.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    title_working: str
    angle: str
    """
    이 주제를 어느 각도로 자를 것인가
    """
    audience: str
    key_message: str
    """
    독자가 단 하나만 기억한다면
    """
    supporting_points: Annotated[list[str], Field(max_length=7, min_length=3)]
    evidence_needed: list[str]
    """
    검증이 필요한 주장 목록
    """
    cta_intent: str
    constraints: Constraints
    source_fidelity: SourceFidelity
    """
    strict이면 원문에 없는 사실을 창작하지 않는다. 문서 입력의 기본값은 strict.
    """
