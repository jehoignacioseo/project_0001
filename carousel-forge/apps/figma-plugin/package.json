{
  "name": "carousel-forge-figma-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "manifest.json을 읽어 Figma에 편집 가능한 캐러셀을 만든다",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types src/*.test.ts"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "@types/node": "^26.2.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
